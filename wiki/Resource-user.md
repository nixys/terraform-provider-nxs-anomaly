# anomaly_user (Resource)

Manages a nxs-anomaly user.

## Example Usage

```terraform
resource "anomaly_user" "alice" {
  name     = "Alice Smith"
  username = "alice.smith"
  email    = "alice@example.com"
  timezone = "Europe/Moscow"

  notification_targets = [
    {
      channel = "telegram"
      target  = "123456789"
    },
    {
      channel = "webhook"
      target  = "https://hooks.example.com/notify"
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Full display name of the user.

### Optional

- `email` (String)
- `locale` (String) UI language for this person. Empty means the UI follows their browser.
- `notification_policies` (Attributes) Personal notification policies for default and important alerts. (see [below for nested schema](#nestedatt--notification_policies))
- `notification_targets` (Attributes List) List of notification targets for this user. (see [below for nested schema](#nestedatt--notification_targets))
- `on_duty` (Boolean)
- `phone` (String)
- `priority` (String) Notification priority: low, medium, high.
- `role` (String) Login role: viewer, responder, editor, admin; empty disables login permissions.
- `telegram_id` (String)
- `timezone` (String) IANA timezone name.
- `username` (String) Login username. Defaults to lower-cased name with spaces replaced by dots.

### Read-Only

- `created_at` (String)
- `id` (String) The ID of this resource.
- `provisioned_by` (String) Set to "terraform" for objects this provider created. The API then refuses edits from anyone else and the web UI disables its own controls, so an out-of-band change cannot be silently undone by the next apply.
- `updated_at` (String)

<a id="nestedatt--notification_policies"></a>
### Nested Schema for `notification_policies`

Optional:

- `default` (Attributes List) (see [below for nested schema](#nestedatt--notification_policies--default))
- `important` (Attributes List) (see [below for nested schema](#nestedatt--notification_policies--important))

<a id="nestedatt--notification_policies--default"></a>
### Nested Schema for `notification_policies.default`

Required:

- `channel` (String)

Optional:

- `target` (String)
- `wait_minutes` (Number)


<a id="nestedatt--notification_policies--important"></a>
### Nested Schema for `notification_policies.important`

Required:

- `channel` (String)

Optional:

- `target` (String)
- `wait_minutes` (Number)



<a id="nestedatt--notification_targets"></a>
### Nested Schema for `notification_targets`

Required:

- `channel` (String) Channel type (webhook, telegram, email, sms, phone).
- `target` (String) Channel-specific target address (URL, chat ID, email, phone number).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Replace object-id with the existing object identifier.
terraform import anomaly_user.alice object-id
```
