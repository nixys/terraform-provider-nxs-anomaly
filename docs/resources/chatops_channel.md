---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "anomaly_chatops_channel Resource - nxs-anomaly"
subcategory: ""
description: |-
  Manages a nxs-anomaly ChatOps channel.
---

# anomaly_chatops_channel (Resource)

Manages a nxs-anomaly ChatOps channel.

## Example Usage

```terraform
resource "anomaly_chatops_channel" "ops_telegram" {
  platform              = "telegram"
  name                  = "ops-alerts"
  team_id               = anomaly_team.ops.id
  commands_enabled      = true
  notifications_enabled = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Channel name or identifier on the platform.
- `platform` (String) ChatOps platform: telegram, slack, mattermost.

### Optional

- `commands_enabled` (Boolean)
- `external_id` (String) Channel identifier used by the external chat platform.
- `notifications_enabled` (Boolean)
- `team_id` (String) ID of the team that owns this channel.
- `user_id` (String) ID of the user that owns this channel (for direct channels).
- `webhook_url` (String, Sensitive) Incoming webhook URL or env: secret reference used for outbound ChatOps messages.

### Read-Only

- `created_at` (String)
- `id` (String) The ID of this resource.
- `provisioned_by` (String) Set to "terraform" for objects this provider created. The API then refuses edits from anyone else and the web UI disables its own controls, so an out-of-band change cannot be silently undone by the next apply.
- `updated_at` (String)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Replace object-id with the existing object identifier.
terraform import anomaly_chatops_channel.ops_telegram object-id
```
